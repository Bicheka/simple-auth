{
  "name": "simple-auth",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:up": "docker compose up -d"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8"
  }
}
